<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Activity types config -->
    <record id="mail_activity_type_action_config_subscription" model="ir.actions.act_window">
        <field name="name">Activity Types</field>
        <field name="res_model">mail.activity.type</field>
        <field name="view_mode">list,form</field>
        <field name="domain">['|', ('res_model', '=', False), ('res_model', '=', 'solt.subscription')]</field>
        <field name="context">{'default_res_model': 'solt.subscription'}</field>
    </record>

</odoo>
